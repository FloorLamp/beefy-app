const styles = theme => ({
  icon: {
    width: '48px',
    height: '48px',
    color: theme.palette.text.primary,
    pointerEvents: 'none',

    '& i': {
      fontSize: '24px',
    },
  },
});

export default styles;
